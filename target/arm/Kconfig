config ARM
    bool
    select ARM_COMPATIBLE_SEMIHOSTING

config AARCH64
    bool
    select ARM

config ARM_V4
    bool
    depends on TCG && ARM

config ARM_V5
    bool
    depends on TCG && ARM

config ARM_V6
    bool
    depends on TCG && ARM

config ARM_V7R
    bool
    depends on TCG && ARM

config ARM_V7M
    bool
    depends on TCG && ARM
    select PTIMER
